# CLAUDE.md

本リポジトリのエージェント向け指示の正本は **`AGENTS.md`**(Codex と共用)。Claude Code もそれに従うこと。

追加の注意:

- 素材の移植元 `hassy0511/bouken-drill` は**読み取り専用**。1ファイルも変更・削除しない
- 明示的に「GO」が出るまで実装(ファイル作成・編集)をせず、設計・提案で止める運用ルールがある(開発者共通ルール)。ただし本リポジトリの `docs/` 整備・更新は承認済みの作業
- 回答の最後に必ず「依頼事項」をまとめること
