# AGENTS.md — ダジャーレせんちょうと こくごの ぐんとう

このリポジトリで作業する AI エージェント(Codex 等)への指示書。**作業開始前に必ず本ファイルと `docs/` の設計書を読むこと。**

## 1. プロジェクト概要(1分版)

小学生向けの**国語専用**学習ゲーム。ダジャレ好きの海賊「ダジャーレせんちょう」一味から、問題を解いて宝を取り返す。

- 対象: 小学1〜6年生(まず1年生から実装)。開発者の子どもが iPad で毎日使う実運用プロダクト
- カリキュラム: **学習指導要領(平成29年告示)準拠**。教科書会社の作品・単元名には依存しない
- 構造: 1学年 = 1海域 = 1販売単位。各海域に5島(もじ/かんじ/ことば/よみとき/かきかた)
- 技術: **Phaser 3 + TypeScript + Vite**、PWA、GitHub Pages 配信(詳細 `docs/01`)

## 2. 設計書の読み順

| # | ファイル | 内容 |
|---|---|---|
| 00 | `docs/00_project_overview.md` | プロダクト定義・ユーザー・販売モデル |
| 01 | `docs/01_tech_architecture.md` | 技術構成・リポジトリ構造・ビルド/デプロイ |
| 02 | `docs/02_game_design.md` | 世界観・キャラ・**ダジャレ品質基準**・ゲームループ |
| 03 | `docs/03_curriculum_framework.md` | 指導要領ベースの学年別スキルマップ(1〜6年) |
| 04 | `docs/04_grade1_design.md` | **1ねんのうみ 詳細設計(5島41ステージ)** ← 最初に実装する範囲 |
| 05 | `docs/05_data_schema.md` | データスキーマ(海域/島/ステージ/プール/セーブ) |
| 06 | `docs/06_question_types.md` | 出題タイプ仕様(操作・採点・演出) |
| 07 | `docs/07_art_guidelines.md` | キャラクタースタイル / SVG・ラスター規約 |
| 08 | `docs/08_verification.md` | テスト・検証手順(Vitest/Playwright/スクショ) |
| 09 | `docs/09_implementation_plan.md` | **実装計画(PR分割・受け入れ条件)** ← 着手順はここ |

`reference/` は移植元素材(データ・SVG・参考実装)。**参照専用。編集・削除禁止。**

## 3. 絶対ルール

1. **子ども向け文言**: UI・セリフはひらがな中心・分かち書き。子どもを否定する言葉は禁止(「まちがい」より「おしい!」)。不正解時に敵キャラを出さない(コンパスの励ましのみ)。
2. **ダジャレ品質**: セリフのダジャレは `docs/02` の4基準(音の重なり2モーラ以上/両方の意味が子どもに分かる/ひらがなで読める/文脈に乗る)を**全て**満たすこと。満たさないダジャレを書かない。
3. **ID・セーブ互換**: 一度リリースしたステージ `id`・localStorage キー・進捗スキーマを変更しない(子どもの進捗が飛ぶ)。スキーマ変更はマイグレーション必須。
4. **権利**: 教科書掲載作品・市販ドリルの文章/イラスト/固有形式を模倣・転載しない。読解文はオリジナル自作、昔話はパブリックドメインの自作再話のみ。フォント・音源は同梱可能なライセンス(OFL等)のみ。
5. **外部依存**: 実行時に外部ネットワークへ依存しない(CDN読み込み禁止・全アセット同梱)。オフライン(PWA)で完全動作すること。
6. **アート品質**: 絵文字をゲーム内アイコンに使わない。SVGは `docs/07` の作法(輪郭線・2階調セルシェーディング・グラデ禁止)に従う。キャラクターの新規作成・表情差分・ポーズ差分は `art/prompts/character-mascot-style.md` を必ず参照する。問題用の絵カードはキャラクター画風を流用せず、`docs/07` の問題用ラスター画像規約に従う。
7. **検証必須**: 実装 PR は `docs/08` の検証(ユニットテスト+Playwright スクリーンショット)を通し、**スクショを PR に添付**すること。

## 4. 技術規約(要点)

- TypeScript strict。ゲームは Phaser 3 のシーンで構成。React 等の UI フレームワークは導入しない
- 「出題ロジック(gen)」と「素材データ(JSON)」の**2層分離**を維持。新しい問い方 = gen/qtype 追加、量産 = データ追加(コード変更なし)
- 出題タイプはプラグイン構造(`src/engine/qtypes/`)。共通契約は `docs/06`
- コミットは Conventional Commits(`feat:` `fix:` `docs:` …)。PR は小さく、`docs/09` の分割に従う

## 5. コミュニケーション規約(開発者との対話)

- 大きな仕様変更・設計変更は、先に `docs/` を更新して提案し、承認を得てから実装する
- **回答の最後に必ず「依頼事項(次に何をしてほしいか)」を簡潔にまとめる**(毎回)
- 実装済み内容の報告には、動くもの(スクショ・URL)を添える
